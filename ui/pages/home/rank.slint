import { QImage } from "../../components/qimage.slint";
export component Rank inherits Rectangle {

    QImage {
        width: 230px;
        height: 230px;
        source: @image-url("../../../assets/404.png");
    }
}