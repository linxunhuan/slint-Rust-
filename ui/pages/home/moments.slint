import { QImage } from "../../components/qimage.slint";
export component Moments inherits Rectangle {
    
    QImage {
        width: 230px;
        height: 230px;
        source: @image-url("../../../assets/sing.png");
    }
}