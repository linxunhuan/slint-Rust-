export component Recently inherits Rectangle {
    // background: black;

    HorizontalLayout {
        alignment: center;

        VerticalLayout {
            alignment: center;

            Image {
                width: 230px;
                height: 230px;
                source: @image-url("../../../assets/recently.png");
            }
        }
    }
}